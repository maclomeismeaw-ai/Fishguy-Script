local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")

local player = Players.LocalPlayer

local gui = Instance.new("ScreenGui")
gui.Name = "FishguyIntro"
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

local bg = Instance.new("Frame")
bg.Parent = gui
bg.Size = UDim2.new(1,0,1,0)
bg.BackgroundColor3 = Color3.fromRGB(0,0,0)
bg.BorderSizePixel = 0

local holder = Instance.new("Frame")
holder.Parent = bg
holder.AnchorPoint = Vector2.new(0.5,0.5)
holder.Position = UDim2.new(0.5,0,0.42,0)
holder.Size = UDim2.new(0,180,0,180)
holder.BackgroundColor3 = Color3.fromRGB(255,255,255)
holder.BorderSizePixel = 0

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(1,0)
corner.Parent = holder

local img = Instance.new("ImageLabel")
img.Parent = holder
img.Size = UDim2.new(1,0,1,0)
img.BackgroundTransparency = 1
img.Image = "rbxassetid://72349539200638"
img.ScaleType = Enum.ScaleType.Crop

local imgCorner = Instance.new("UICorner")
imgCorner.CornerRadius = UDim.new(1,0)
imgCorner.Parent = img

local stroke = Instance.new("UIStroke")
stroke.Parent = holder
stroke.Thickness = 3
stroke.Color = Color3.fromRGB(255,255,255)

local title = Instance.new("TextLabel")
title.Parent = bg
title.AnchorPoint = Vector2.new(0.5,0.5)
title.Position = UDim2.new(0.5,0,0.65,0)
title.Size = UDim2.new(0,500,0,50)

title.BackgroundTransparency = 1
title.Text = "Fishguy Script"
title.TextColor3 = Color3.fromRGB(255,255,255)
title.TextScaled = true
title.Font = Enum.Font.Arcade

local discord = Instance.new("TextLabel")
discord.Parent = bg
discord.AnchorPoint = Vector2.new(0.5,0.5)
discord.Position = UDim2.new(0.5,0,0.71,0)
discord.Size = UDim2.new(0,600,0,40)

discord.BackgroundTransparency = 1
discord.Text = "Discord : discord.gg/WyVueshvB"
discord.TextColor3 = Color3.fromRGB(180,180,180)
discord.TextScaled = true
discord.Font = Enum.Font.Arcade

bg.BackgroundTransparency = 1
holder.BackgroundTransparency = 1
img.ImageTransparency = 1
title.TextTransparency = 1
discord.TextTransparency = 1
stroke.Transparency = 1

TweenService:Create(bg,TweenInfo.new(1),{
    BackgroundTransparency = 0
}):Play()

TweenService:Create(holder,TweenInfo.new(1),{
    BackgroundTransparency = 0
}):Play()

TweenService:Create(img,TweenInfo.new(1),{
    ImageTransparency = 0
}):Play()

TweenService:Create(title,TweenInfo.new(1),{
    TextTransparency = 0
}):Play()

TweenService:Create(discord,TweenInfo.new(1),{
    TextTransparency = 0
}):Play()

TweenService:Create(stroke,TweenInfo.new(1),{
    Transparency = 0
}):Play()

task.spawn(function()
    while gui.Parent do
        img.Rotation += 2
        task.wait()
    end
end)

task.wait(5)

TweenService:Create(bg,TweenInfo.new(1.5),{
    BackgroundTransparency = 1
}):Play()

TweenService:Create(holder,TweenInfo.new(1.5),{
    BackgroundTransparency = 1
}):Play()

TweenService:Create(img,TweenInfo.new(1.5),{
    ImageTransparency = 1
}):Play()

TweenService:Create(title,TweenInfo.new(1.5),{
    TextTransparency = 1
}):Play()

TweenService:Create(discord,TweenInfo.new(1.5),{
    TextTransparency = 1
}):Play()

TweenService:Create(stroke,TweenInfo.new(1.5),{
    Transparency = 1
}):Play()

task.wait(2)

gui:Destroy()